apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: backstage-orchestrator-token
  labels:
    {{- include "developer-hub.labels" . | nindent 4 }}
  annotations:
    {{- include "developer-hub.argocd-syncwave" . | nindent 4 }}
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-secret-store
    kind: ClusterSecretStore
  target:
    name: backstage-orchestrator-token
    creationPolicy: Owner
  data:
    - secretKey: ORCHESTRATOR_TOKEN
      remoteRef:
        key: secrets/rhdh/orchestrator-token
        property: value
